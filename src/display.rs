pub mod display {

    pub struct GameWindow {
        width: u32,
        height: u32,
    }

    impl GameWindow{
        pub fn new(width: u32, height: u32) -> Self {
            todo!()
        }
    }

    impl Default for GameWindow {
        fn default() -> Self {
        todo!()

        }
    }
}
